export function formatCRC(value: string): string { const normalized = value.trim().replace(',', '.'); if (!/^(?:0|[1-9]\d{0,15})(?:\.\d{1,2})?$/.test(normalized)) return '₡0.00'; const [integerPart, decimalPart = ''] = normalized.split('.'); const grouped = integerPart.replace(/\B(?=(\d{3})+(?!\d))/g, '.'); const parts = new Intl.NumberFormat('es-CR', { style: 'currency', currency: 'CRC', minimumFractionDigits: 2, maximumFractionDigits: 2 }).formatToParts(0); return parts.map(({ type, value }) => type === 'integer' ? grouped : type === 'fraction' ? decimalPart.padEnd(2, '0') : value).join(''); }
export function signedCRC(amount: string, direction: 'INFLOW' | 'OUTFLOW'): string { return `${direction === 'INFLOW' ? '+' : '-'}${formatCRC(amount)}`; }
