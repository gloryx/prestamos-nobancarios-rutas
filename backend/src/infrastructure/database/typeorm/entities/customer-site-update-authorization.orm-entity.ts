import { Column, CreateDateColumn, Entity, Index, PrimaryGeneratedColumn } from 'typeorm';
@Entity({ name: 'customer_site_update_authorizations' })
@Index('idx_customer_site_auth_customer', ['customerId', 'authorizedAt'])
export class CustomerSiteUpdateAuthorizationOrmEntity { @PrimaryGeneratedColumn('uuid') id!: string; @Column({ name: 'customer_id', type: 'uuid' }) customerId!: string; @Column({ name: 'collector_user_id', type: 'uuid' }) collectorUserId!: string; @Column({ type: 'varchar' }) scope!: string; @Column({ type: 'text' }) reason!: string; @Column({ name: 'authorized_by_user_id', type: 'uuid' }) authorizedByUserId!: string; @CreateDateColumn({ name: 'authorized_at', type: 'timestamptz' }) authorizedAt!: Date; @Column({ name: 'expires_at', type: 'timestamptz' }) expiresAt!: Date; @Column({ name: 'used_at', type: 'timestamptz', nullable: true }) usedAt!: Date | null; @Column({ name: 'revoked_at', type: 'timestamptz', nullable: true }) revokedAt!: Date | null; }
